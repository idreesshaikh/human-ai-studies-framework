# Start your session

Once connected, the pre-flight summary tells you exactly what will and
will not be captured before the clock starts. Start when you're ready —
the timer then lives quietly in the status bar.
