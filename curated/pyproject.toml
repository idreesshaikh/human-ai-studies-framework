[project]
name = "curated"
version = "0.1.0"
description = "Curated-dataset leg: mining adapters, normalizer, heuristics, validity-threats record (FR-CUR-*)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/curated"]
