# Traceability matrix (living document)

Updated at the end of every mega-prompt (`docs/archive/roadmap/`). A phase is complete
only when its rows here are flipped. Status: ✅ done · 🔶 partial · ⬜ open ·
- deliberately not built (Won't / superseded).

## 1. Requirement → source → implementation

| Requirement | Traces to (RQ / stakeholder / source) | Component | Implementing phase | Status |
| ----------- | ------------------------------------- | --------- | ------------------ | ------ |
| FR-PROT-1 | RQ-F1; S1,S3,S5 | `protocol/` schema | MP-02 | ✅ |
| FR-PROT-2 | RQ-F1; S6 | `protocol/` schema | MP-02 | ✅ |
| FR-PROT-3 | RQ-F1,F2; S1,S3 | `protocol/lifecycle.py` | MP-02 | ✅ |
| FR-PROT-4 | RQ-F1; S1 | `protocol/cli.py` derive | MP-02 | ✅ |
| FR-PROT-5 | RQ-F2; S1,S4 | protocol analysis plan | MP-02 | ✅ |
| ~~FR-PROT-6~~ | superseded by FR-LIT-1/3 (2026-07-11) | - | - | - |
| FR-PROT-7 | RQ-F3; S5 | replication kit export | MP-09 | ✅ (`protocol export replication-kit`; byte-identical reproduction test) |
| FR-PROT-8 | (deferred vision) | - | - | - |
| FR-INST-1 | RQ-P1; S1 | Cognitive Overlay `surveys/fatiguePrompt` | built pre-roadmap | ✅ |
| FR-INST-2 | RQ-P1; S1 | Cognitive Overlay `stuckDetector/stuckPrompt` | built pre-roadmap | ✅ |
| FR-INST-3 | RQ-P1; S1,S2 | Cognitive Overlay `session/endSurvey` | built pre-roadmap | ✅ |
| FR-INST-4 | RQ-P2; S1; metrics/docs/static_code_metrics.md | `metrics/` | MP-03 | ✅ 9/9 (cognitive complexity stub-degradable per D5) |
| FR-INST-5 | RQ-P3; S1,S2; extension/docs/developer_behavior_capture.md | Cognitive Overlay behavioral leg | MP-05 | ✅ |
| FR-INST-6 | RQ-F1; all legs | overlay ✅ / metrics join keys | MP-03 | 🔶 overlay ✅ (incl. behavioral leg, MP-05), metrics ✅; agent leg with MP-12 |
| FR-INST-7 | (deferred vision); S6 | - | - | - |
| FR-INST-8 | RQ-P4; D2 (Tako) | overlay AI-lifecycle events | MP-05 | ✅ (public-API mechanism; blind spots in `extension/docs/adaptation-notes.md`) |
| FR-INST-9 | RQ-P4; behavior doc §visible ranges | overlay visible-range tracker | MP-05 | ✅ |
| FR-INST-10 | RQ-P3,P4 | overlay origin classifier (`src/core`) | MP-05 | ✅ |
| FR-INST-11 | RQ-P3 denominators; D4 (WakaTime) | overlay heartbeat/idle (`src/core`) | MP-05 | ✅ |
| FR-INST-12 | S2/S3; D4 | overlay capture filters | MP-05 | ✅ |
| FR-INST-13 | S2; NFR-1; "glass overlay" elicitation | overlay prompt surfaces | MP-05 (styling pass) | ✅ in-editor everywhere; debrief glass ✅; fatigue probe stays native QuickPick - no stable overlay-webview API exists (constraint documented in `extension/docs/adaptation-notes.md`) |
| FR-INST-14 | S5; replication provenance | overlay environment_snapshot event | MP-05 | ✅ |
| FR-INST-15 | RQ-P2 time series; D14 | workspace snapshotter (shadow git) | MP-12 | ✅ |
| FR-INST-16 | outcome ground truth; RQ-P1–P5 | task harness | MP-12 | ✅ |
| FR-INST-17 | RQ-P2 trajectory, RQ-P3/P4; metric-coverage.md; D14 | code-evolution derivation + `git_commit` events (snapshotter) | MP-12 | ✅ derivation + content-free `git_commit` built; churn/persistence **char-approximated** (line-level pending schema-v4 burst line ranges - deferred, below) |
| FR-INST-18 | RQ-P1/P3 struggle proxy; metric-coverage.md | overlay diagnostics/health stream | post-sprint (schema v4) | ⬜ |
| FR-AGENT-1 | RQ-P5; S1 | agent leg event contract | MP-12 | ✅ |
| FR-AGENT-2 | RQ-P5; D13 | Claude Code hooks + transcript importer | MP-12 | ✅ |
| FR-AGENT-3 | RQ-P5; strengthens FR-INST-10 | correlation job (reliance loops + burst annotation) | MP-12 | ✅ |
| FR-AGENT-4 | S6; extension point | Copilot/generic import adapters | deferred (post-sprint) | ⬜ |
| FR-AGENT-5 | S2,S3; C1 | content-policy redactor + consent generator | MP-12 | ✅ |
| FR-ING-1 | S1; NFR-2; D3 (ActivityWatch) | `middleware/` :8000 ingest | MP-04 | ✅ |
| FR-ING-2 | NFR-2; S1 | `middleware/` | MP-04 | ✅ |
| FR-ING-3 | NFR-2; S1 | `middleware/` gap report | MP-04 | ✅ |
| FR-ING-4 | RQ-P1–P4; S1,S5 | `middleware/` dataset export | MP-04 | ✅ |
| FR-ING-5 | S3; FR-ETH-1; FR-LIT-1 | `middleware/` file store | MP-04 | ✅ |
| FR-ING-6 | RQ-F2 | `middleware/` protocol check | MP-04 | ✅ |
| FR-DASH-1 | S1 | `dashboard/` overview | MP-06 | ✅ |
| FR-DASH-2 | RQ-F1; S1,S3 | `dashboard/` lifecycle board | MP-06 | ✅ |
| FR-DASH-3 | S1; C2 | `dashboard/` live view | MP-06 | ✅ |
| FR-DASH-4 | RQ-F1; S1,S4 | `dashboard/` timeline | MP-06 | ✅ (agent lane renders when MP-12 data lands) |
| FR-DASH-5 | RQ-P2; S1 | `dashboard/` metrics view | MP-06 | ✅ |
| FR-DASH-6 | RQ-F2; S4 | `dashboard/` chips | MP-06 | ✅ |
| FR-DASH-7 | supervisor; S1; RQ-F2 | `dashboard/` task board (project manager) | MP-06 | ✅ |
| FR-DASH-8 | S1 | `dashboard/` knowledge views | MP-06 + MP-10 | ✅ graph + assistant panels built (MP-10) |
| FR-DASH-9 | S6,S7; owner onboarding gap (2026-07-12) | `dashboard/` guided tour + lexicon tooltips; middleware `/requirements` + `/glossary` | post-sprint enhancement | ✅ |
| FR-LIT-1 | S1,S5; D8 | middleware paper ingest + extraction | MP-10 | ✅ id path (S2) + PDF path (pymupdf, D21); FTS5 index |
| FR-LIT-2 | S1; D7,D8 | literature graph (S2 API + dashboard) | MP-10 | ✅ edge harvest + stub nodes + force-graph panel |
| FR-LIT-3 | RQ-F2; S4 | paper↔protocol links | MP-10 | ✅ seeded from protocol `literature:`, editable in drawer |
| FR-LIT-4 | S1,S4; D10; FR-ETH-4 | knowledge assistant (Claude API) | MP-10 | ✅ tool-use loop, cited answers, graceful without key |
| FR-LIT-5 | S5; D9 | Zotero import | MP-09 | ✅ `POST /studies/{id}/papers/zotero-import` (local API + web fallback); papers keyed by canonical paperRef so protocol links join |
| FR-ANA-1 | RQ-F3; S5,S6 | `analysis/core.py` | MP-07 | ✅ |
| FR-ANA-2 | RQ-F2 | `analysis/` requires-check | MP-07 | ✅ |
| FR-ANA-3 | RQ-P1–P4 | built-in recipes (incl. `ai-review-behavior`) | MP-07 | ✅ (agent/outcome recipes run once MP-12 data lands) |
| FR-ANA-4 | RQ-F2; S1,S4 | `analysis/` runner+report | MP-07 | ✅ |
| FR-ANA-5 | RQ-F3 | replicated-paper recipe | MP-07 / MP-09 | ✅ two cited replications: `ziegler-acceptance-rate` (MP-07) + `meyer-fragmentation` (MP-09) |
| FR-ANA-6 | S1,S4; "extract a paper" | paper-draft generator | MP-11 | ✅ MP-11 |
| FR-META-1 | RQ-F2 | operational findings log (middleware) | MP-04 + MP-11 | ✅ MP-11 (endpoints + ingest flags MP-04; facilitator findings DR-01..07 MP-08; auto-scan seq-gap/gate-block + recipe requires-fail rows + dashboard finding cards MP-11) |
| FR-META-2 | RQ-F2; D10 | retrospective (Claude-assisted, human-approved) | MP-11 | ✅ MP-11 |
| FR-OPS-1 | S6,S7; NFR-5 boundary; student-benefit cost constraint | `render.yaml` (demo, D25) + `deploy/compose.prod.yml` + `deploy/Caddyfile` (VM, D26) + `middleware/scripts/start_with_seed.sh` | post-sprint (2026-07-16) | 🔶 manifests + pipeline built; Render/Azure provisioning pending (RUNBOOK §9) |
| FR-OPS-2 | change management; RQ-F3; NFR-9 | `.github/workflows/release.yml` + `deploy.yml` (GHCR, D24) | post-sprint (2026-07-16) | 🔶 built; first tagged release pending |
| FR-OPS-3 | S6,S7; FR-PROT-4 | `release.yml` marketplace job (D27); `extension/package.json` publisher/repository | post-sprint (2026-07-16) | 🔶 built; publisher account + `VSCE_PAT` pending |
| FR-OPS-4 | D5; zero-idle-cost constraint | `.github/workflows/sonar-vm.yml` + compose `--profile sonar` | post-sprint (2026-07-16) | 🔶 workflow built; sonar VM provisioning pending |
| FR-OPS-5 | owner elicitation 2026-07-16; NFR-1 (ingest open); D29 | `middleware/auth.py` (none/token/clerk, 14 tests) + `GET /auth/config` + dashboard `SignIn.svelte` | post-sprint (2026-07-16) | 🔶 seam + token sign-in built + tested; Clerk client widget pending provisioning |
| FR-OPS-6 | owner elicitation 2026-07-16; D30 rev 2; D15 | dashboard `api.ts` (`VITE_API_BASE`) + `settings.cors_origins` → opt-in `CORSMiddleware` (off-by-default regression test) | post-sprint (2026-07-16) | ✅ |
| FR-ETH-1 | S3,S2 | lifecycle gates + file store | MP-02 + MP-04 | ✅ (gates MP-02; file store MP-04) |
| FR-ETH-2 | S2,S3; C1 | all instruments | overlay ✅ incl. behavioral leg (MP-05); agent leg (MP-12) | ✅ built instruments comply (sizes/shapes/timings + salted in-memory hashes only; clipboard never read); agent-leg scoped exception implemented under FR-AGENT-5 (MP-12): conversation text passes one choke point (`redact.py`), default `metadata-only` stores zero text (grep-the-output test); snapshots content-free; `git_commit`/tool targets are hashes/counts, never message text or paths |
| FR-ETH-3 | S2,S3 | all components | by construction | ✅ |
| FR-ETH-4 | S2,S3; NFR-5 | assistant data boundary | MP-10 | ✅ enforced server-side in `assistant.py`: no tool returns a row-level event; grep-the-output test |
| NFR-1 | S2; C2; D3 | all instruments | overlay ✅ incl. behavioral leg (MP-05) | ✅ all sensors fire-and-forget, O(1) handlers, swallow-count-report-once; wrapper commands always delegate in `finally` |
| NFR-2 | S1 | sinks + middleware | overlay ✅; MP-04 | ✅ (idempotent ingest + visible seq gaps) |
| NFR-3 | S6; comparability | Cognitive Overlay core/adapter split | built; guarded in MP-05 (burst/origin/idle/filter/debounce logic all in `src/core`, zero `vscode` imports) | ✅ |
| NFR-4 | S6; C4 | schema versions, recipe contract | MP-02,04,07 | 🔶 overlay ✅; `protocolVersion` ✅ (MP-02); event `v` stored + unknown flagged (MP-04) |
| NFR-5 | S2,S3 | whole stack | binds MP-04,05,10 | 🔶 |
| NFR-6 | S5 | recipes, pinned deps | MP-07, MP-09 | ✅ recipes deterministic given a dataset (seeded jitter, no wall-clock in outputs); pinned via uv lock (D16, D20); replication kit reproduces `report.md` byte-for-byte and exports byte-identical archives (MP-09) |
| NFR-7 | S1 | docker compose packaging | MP-04, MP-06, MP-08 | ✅ compose + demo seed (MP-04); SPA served by the middleware image, one process (MP-06); full-stack proof `scripts/smoke.sh` green from clean `docker compose up -d --build` (MP-08) |
| NFR-8 | S4 | recipe statistics | MP-07 | ✅ enforced by construction in `analysis/stats.py` (every test line carries exact test, effect size, per-cell n, small-n framing) |
| NFR-9 | production sprint | compose + demo seed + smoke test | MP-08 (day 7) | ✅ `scripts/smoke.sh` green from `docker compose up -d --build`: health → dashboard → idempotent replay → dataset export → per-RQ report |
| NFR-10 | S4 | `build-vs-adopt.md` | MP-01 (this revision) | ✅ D1–D12 recorded |
| NFR-11 | S6,S7; open-source posture; owner elicitation 2026-07-16 | README, CONTRIBUTING, RUNBOOK, TOUR, dashboard copy; internals → `docs/archive/` | post-sprint (2026-07-16) | 🔶 docs restructured; dashboard chip inversion pending |

## 2. RQ → data elements → recipes (analysis coverage)

| RQ | Data elements (event types / columns) | Recipes | Status |
| -- | ------------------------------------- | ------- | ------ |
| RQ-P1 | `fatigue_response`, stuck events, debrief payloads | `fatigue-by-condition`, `stuck-episodes`, `tlx-debrief` | 🔶 pipeline proven at dry run (both conditions); participant data pending |
| RQ-P2 | 9-metric columns in `function_metrics` / `file_metrics` + join keys | `code-quality-by-condition` | 🔶 pipeline proven at dry run; participant data pending |
| RQ-P3 | `clipboard_paste`, `edit_burst`(+`origin`), `editor_focus`, `file_save`, heartbeat/idle | `paste-behavior` | 🔶 pipeline proven at dry run; participant data pending |
| RQ-P4 | `ai_suggestion` lifecycle events, `visible_range`, `fatigue_response` join | `ai-review-behavior` | 🔶 pipeline proven at dry run (+ Ziegler replication); participant data pending |
| RQ-P5 | `agent_turn`, `agent_tool_call`, `agent_session_meta`, reliance-loop correlations, `task_outcome` | `agent-interaction-dynamics`, `task-outcome-by-condition` | ⬜ documented gap: uninstrumented until MP-12; fails loudly at plan validation (DR-07) |
| RQ-F1 | protocol → derived configs → gate reports | MP-08 dry run + findings log | 🔶 dry-run answer in `study/pilot/findings.md` §1: protocol drove everything; 3 specification leaks (DR-01/02/03); final pass after sessions |
| RQ-F2 | requires-check failures, operational findings log (FR-META-1), setup-time log | MP-08 `findings.md` + MP-11 retrospective | 🔶 dry-run evidence: RQ-P5 defect caught at plan validation pre-collection; 7 RE-classified findings; setup-time ledger fills during sessions |
| RQ-F3 | replication kit re-import | MP-09 reproduction test | ✅ kit reproduces `report.md` byte-for-byte from a fresh checkout (`protocol/tests/test_export.py` + live CLI demo on the 128-row demo dataset); "replicate studies in a standard way" is now demonstrated for our own study |

## 3. Phase completion log

| Date | Phase | Rows flipped | Notes |
| ---- | ----- | ------------ | ----- |
| 2026-07-11 | MP-01 | matrix created; pre-existing overlay work recorded as ✅ | baseline |
| 2026-07-11 | MP-01 (rev 2) | added FR-INST-8–12, FR-LIT-1–5, FR-META-1–2, FR-DASH-7–8, FR-ANA-6, FR-ETH-4, NFR-9–10; superseded FR-PROT-6; NFR-10 ✅ via build-vs-adopt.md D1–D12 | project renamed "Framework for Conducting Human-AI Studies"; one-week sprint plan adopted; sources: docs/, supervisor input |
| 2026-07-11 | MP-01 (rev 3) | added the agent leg FR-AGENT-1–5 (+ decisions D13–D14), FR-INST-13–16 (glass HUD, environment snapshot, workspace snapshots, task harness), RQ-P5; amended FR-ETH-2 (rev 2: consent-scoped exceptions) and FR-ANA-3 (two new recipes) | gap elicited by Idrees: the agent's side of the interaction was uninstrumented; pilot AI condition standardized on Claude Code in the integrated terminal |
| 2026-07-11 | MP-04 | FR-ING-1–6 ✅; FR-ETH-1 ✅ (file store completes MP-02's gates); NFR-2 ✅; FR-META-1 🔶 (findings endpoints + auto-logged ingest flags); NFR-4/NFR-7 🔶 advanced; SRS status column synced with this matrix | FastAPI+SQLite service, idempotent on `(sessionId, seq)` (events) / content hash (metrics), gap report, one-timeline dataset (JSON+CSV), content-addressed file store, `/findings` + `/tasks` reserved groups, Dockerfile + compose (demo-seed, sonar profile), replay smoke script; 17 API tests; live replay verified (11 events + deliberate gap detected + 4 metric rows joined). **Deviations:** src layout `middleware/src/middleware/` instead of the mega-prompt's `middleware/app/` (repo convention wins); participant validation uses the documented `P1..P<planned>` convention since the protocol declares a count, not an ID roster |
| 2026-07-11 | MP-01 (re-verification) | none flipped; FR-ING-2 source strengthened (NFR-2; S1) | full audit re-run per MP-01's verification section: SRS ↔ traceability 70/70 IDs identical both directions; every roadmap-cited ID exists in the SRS; every RQ id used anywhere is defined in research-questions.md; every Must has an implementing phase; glossary spot-check clean. MP-02/03/07 RE-traceability sections now cite exact requirement IDs |
| 2026-07-11 | MP-02 | FR-PROT-1–5 ✅; FR-ETH-1 🔶 (gate side); NFR-4 🔶 (`protocolVersion` const, consumers branch on it) | schema + loader with referential checks, lifecycle gates, CLI (`validate`/`status`/`derive overlay-settings`), pilot example, 28 tests; derive output verified key-for-key against the extension's contributed settings |
| 2026-07-11 | MP-03 | FR-INST-4 ✅ (9/9 metrics); FR-INST-6 🔶 (metrics leg now stamps join keys + `schemaVersion`) | orchestrator `metrics/src/main.py` emits `function_metrics` + `file_metrics` as CSV or JSONL with `--participant/--condition/--session`; sonar stub degrades to NaN with one warning (D5). **Deviation from the implementation plan's "keep the four existing metric functions untouched":** the existing capture pairing (`zip` over flat `captures()` lists) misassigned parameter counts / identifier lengths / scope distances on files with methods or nested functions - rewritten on per-match pairing (`matches()`), values hand-verified against `metrics/corpus/` signatures and the `radon` CLI; duplicate function names now uniformly keep the first occurrence. Stale `__pycache__`/dead `src/metrics/` dir removed |
| 2026-07-11 | repo restructure | decisions D15 (Svelte, supersedes D11 frontend) + D16 (uv/Python 3.12 workspace) recorded | `Cognitive Overlay/` → `extension/`, `Static Code Metrics/` → `metrics/`; uv workspace with skeleton packages (protocol, middleware, analysis, agent-capture); root contributor guide (PROJECT_GUIDE.md) added; build artifacts and template .gitignore bloat removed; fixed hardcoded path in `metrics/src/parsers/ts_parser.py` |
| 2026-07-11 | MP-05 | FR-INST-5, 8–12, 13 (styling pass), 14 ✅; FR-ETH-2 ✅ (built instruments); NFR-1 ✅; FR-INST-6 🔶 advanced (behavioral leg carries join keys via the shared Recorder) | Behavioral telemetry leg in the Cognitive Overlay (D12): `SCHEMA_VERSION` 2→3; core burst aggregator + origin classifier, idle state machine, capture filter, debouncers (`extension/src/core/`, 33 new mocked-timer tests, 62 total); adapter + `cognitiveOverlay.behavior.*` settings; middleware now recognizes schema v3 (`KNOWN_EVENT_SCHEMA_VERSIONS`); `extension/docs/adaptation-notes.md` records what was adapted/rejected from Tako/ActivityWatch/WakaTime (NFR-10). **Deviations:** (1) paste/AI-accept interception uses session-scoped keybinding wrapper commands that delegate to the built-ins, NOT a shadowing `registerCommand` of `editor.action.clipboardPasteAction` - Tako's monkey-patching was studied and rejected as brittle, and none of the three references intercepts paste at all (mechanism + blind spots documented); (2) in the origin ranking, the block-size heuristic ranks BELOW paste correlation (a large paste satisfies both; direct evidence wins) - undo-redo > ai-correlated > paste > ai-block > human; (3) FR-INST-13 fatigue probe stays a native QuickPick: no stable VS Code API for overlay webviews (constraint documented, NFR-1 invariant preserved); (4) the acceptance scenario ran scripted against the compiled core + real JSONL/HTTP sinks + live middleware (all 8 event types, origins human/paste/ai, idle pair, 17/17 ingested, 0 flags, replay idempotent, non-Python edit → zero events) - an interactive dev-host pass remains advisable before the pilot |
| 2026-07-11 | MP-06 | FR-DASH-1–7 ✅; FR-DASH-8 🔶 (knowledge route + mount-point stubs; panels MP-10); NFR-7 🔶 advanced (SPA built into the middleware image, one process serves the stack); decisions D17–D19 recorded | Svelte 5 + Vite + TS SPA in `dashboard/` (D15): hand-rolled history router over `/study/:id/{overview\|board\|tasks\|live\|sessions/:sid\|metrics\|knowledge}`; task board derives cards purely from the new `GET /studies/:id/status` (projection - cards self-clear), manual cards via existing `/tasks`; lifecycle board computed via new `GET /studies/:id/lifecycle` (uploads clear gates in-UI); swimlane timeline (lanes.ts pure assembly: spans/points/bands, origin coloring, brush zoom, payload tooltips, table-view twin, agent lane mapped for MP-12); conversation view (metadata-only rendering until MP-12 data exists, two-way linked with the timeline); metrics compare (jittered points + median, IQR box only at n≥5, per-cell n - NFR-8 honest); trace chips on every view (FR-DASH-6). Middleware additions: `/studies/:id/{protocol,lifecycle,status,live}`, `/files`, optional bearer token (`MIDDLEWARE_TOKEN`, ingest deliberately exempt per NFR-1), SPA serving (`MIDDLEWARE_DASHBOARD`); 24 API tests (was 17) + 20 dashboard vitest tests; demo seed extended (behavioral session S-sample-002: 13 event types / 2 legs; both-condition metric rows; design artifacts uploaded so the pilot computes to its ethics gate). **Deviations:** (1) file dedup key changed from bare sha256 to (filename, sha256) - gates are keyed by filename, and identical bytes under two artifact names must register both (bytes still stored once); (2) bearer token is optional and off in the local/demo deployment (NFR-5) rather than mandatory; (3) verification walked every route + acceptance data over HTTP (all 200, status/live/lifecycle checked); browser screenshots pending a connected Chrome extension |
| 2026-07-12 | MP-07 | FR-ANA-1–4 ✅; FR-ANA-5 🔶 (`ziegler-acceptance-rate` built + cited; second paper recipe MP-09); NFR-8 ✅ (honest by construction); NFR-6 🔶 (deterministic recipes + uv lock; replication kit MP-09); decision D20 (pandas/scipy/matplotlib) recorded | Recipe contract + registry (`analysis/core.py`: id / answers / requires / run, `@recipe` decorator, plan validation naming every missing event type or metric column *before* anything runs); `Dataset` wraps the middleware one-timeline export (fetch or file, events/metrics legs, join keys per FR-INST-6); `analysis/stats.py` centralizes exact Wilcoxon signed-rank / Mann-Whitney U (Cliff's delta) / Fisher / Spearman with per-participant aggregation against pseudo-replication - recipes cannot emit a bare p-value; house figure helpers (`analysis/figures.py`, dashboard palette, every observation drawn, per-cell n on the axis, zero-included scales); 8 built-in recipes + the FR-ANA-5 replication demo (Ziegler et al. MAPS '22, arXiv:2205.06537, acceptance rate + self-report correlation with the fatigue stand-in stated); runner + CLI (`analysis run/validate/list`, exit 2 on validation failures, per-RQ `report.md`, recipe runs recorded to the middleware best-effort so FR-DASH-7 cards clear); 14 pytest (79 repo-wide) incl. end-to-end on a synthetic fixture with constructed known answers (Wilcoxon p=0.125/r=-1.0 recovered, acceptance rate 0.5). Verified against the live replayed demo seed: 7/9 plan entries ran, `agent-interaction-dynamics`/`task-outcome-by-condition` failed validation loudly naming `agent_turn`/`task_outcome` (MP-12 data, correct). **Deviations:** (1) FR-ANA-3's "accept rate by suggestion size quartile" is not computable under event schema v3 - size (`charCount`) is recorded only on *accepted* suggestions - so `ai-review-behavior` reports accepted-size quartiles + overall accept rate and states the schema gap (instrumentation finding for MP-12/schema v4, not papered over); (2) `ziegler-acceptance-rate` added to the pilot protocol's analysisPlan under RQ-P4 (+ literature entry arxiv:2205.06537) so the replication demo is protocol-traceable like every other recipe; (3) recipes degrade to descriptives with an explicit "no comparison possible" note when only one condition is present (the demo seed is ai-assisted-only) rather than failing - honesty over crash |
| 2026-07-12 | MP-08 (in-week slice) | NFR-9 ✅ (`scripts/smoke.sh` + compose + seeded demo); FR-META-1 🔶 advanced (7 facilitator findings DR-01..07, requirement-linked, RE-classified); RQ coverage rows moved to 🔶 pipeline-proven (RQ-P5 stays the documented gap); pilot protocol **frozen v1.0** (45-min sessions, tasks named, `agentCapture: metadata-only` declared pre-freeze per FR-AGENT-5) | Study kit shipped (`study/pilot/`): facilitator runbook (setup derived from the protocol, counterbalancing Latin square, per-session checklists, incident playbook), participant info sheet, consent form matched to FR-ETH-2 rev 2 (per-stream initials incl. the two scoped content exceptions), UoM ethics-application text, matched task designs A "expenses"/B "logbook" (repos + harness land MP-12 per FR-INST-16). Dry run PASS (`study/pilot/dry-run-report.md`, uploaded as the pilot gate artifact): P00's deterministic counterbalanced session pair (34+23 events, zero seq gaps, off-roster rows correctly flagged `unknown-participant`), real metrics-orchestrator runs both conditions, `analysis run` → per-RQ report with two-condition exact tests, lifecycle live-verified blocked at `ethics` while ingest flowed (FR-ETH-1 + NFR-1 acceptance behavior; a satisfied later-phase gate does not leapfrog). Framework post-mortem `study/pilot/findings.md` answers RQ-F1/F2 with dry-run evidence. **Deviations:** (1) dry-run sessions simulated at the HttpSink wire level, not via a human-driven dev-host (DR-06 gates the first participant on one interactive pass); (2) smoke chain ends at report generation - `analysis paper` joins when MP-11 lands; (3) conducted sessions + final traceability pass deferred post-sprint per the mega-prompt itself (ethics gate + recruitment); (4) demo-seed volume must be reset before real data (DR-05, runbook step added) |
| 2026-07-12 | MP-01 (rev 4, metric elicitation) | added FR-INST-17 (S: code-evolution/process metrics - LOC series, within-session churn, AI-code persistence, content-free `git_commit` events; lands MP-12) + FR-INST-18 (C: IDE diagnostics/health stream; post-sprint, schema v4); glossary +Churn +Persistence | Gap elicited by Idrees: "do we capture lines of code added or commits made?" - per-burst line/char deltas with origin existed (FR-INST-5/10), but participant commits, session-level LOC series, churn, and persistence were unspecified. Literature review recorded in `requirements/metric-coverage.md` (grounding: SLR arXiv:2507.03156 - commits/LOC/churn among the most-reported telemetry metrics; METR arXiv:2507.09089 task-time design; Ziegler arXiv:2205.06537 persistence; SPACE framework) mapping every literature metric family (human/code/IDE/agent/economic) to a leg with captured/specified/gap/excluded status - exclusions (keystrokes, screen recording, team-scale metrics) argued, not silent. MP-12 spec extended (`git_commit` event, code-evolution derivation, schema-v4 items: suggestion size at `shown` [DR-01], burst line ranges, transcript token-usage lift); MP-09 second-recipe candidates named (persistence / churn). No sprint-Must scope change (golden rule 6: additions are S/C) |
| 2026-07-12 | MP-09 | FR-PROT-7 ✅; FR-LIT-5 ✅; FR-ANA-5 ✅ (was 🔶); NFR-6 ✅ (was 🔶); RQ-F3 ✅ (was ⬜); D9 annotated (built, stdlib); glossary +Paper set; pilot protocol amended **v1.1** (RQ-P3 gains `meyer-fragmentation`, Meyer literature entry added) | All three MP-09 items done and demo-verified against the live stack. **Item 1 - replication kit (FR-PROT-7, NFR-6):** `protocol export replication-kit` packages the frozen protocol + anonymized dataset + regenerated per-RQ report + schema + recipe/package/schema versions + pinned `uv.lock`/`.python-version` + protocol↔paper links into one byte-stable `.tar.gz` (fixed tar metadata, sorted entries, gzip mtime=0; report regenerated with `SOURCE_DATE_EPOCH=0`). The report is regenerated by invoking the `analysis` CLI as a subprocess (protocol is a lower layer, must not import analysis). Reproduction is the acceptance test: a fresh checkout + the kit regenerates `report.md` **byte-for-byte** - proven by `protocol/tests/test_export.py` and a live CLI demo on the 128-row demo dataset (extract → `analysis run` → empty `diff`), and two exports are byte-identical. This makes "replicate studies in a standard way" a demonstrated RQ-F3 claim for our own study. **Item 2 - Zotero import (FR-LIT-5, D9):** `POST /studies/{id}/papers/zotero-import` reads one collection's top-level items via the Zotero desktop app's local read-only API v3 (`localhost:23119/api/users/0`, no key), falling back to the hosted API (`api.zotero.org` + `MIDDLEWARE_ZOTERO_USER_ID`/`_API_KEY`) when the desktop app is down; both speak identical JSON so one normalizer covers both. Papers land in a new `papers` table keyed by canonical `paperRef` (`doi:`/`arxiv:`/`zotero:<key>` - the protocol's `literature:` scheme), so re-imports are idempotent on `(studyId, paperRef)` and protocol links join by construction (`GET .../papers` flags `inProtocolLiterature`). `GET /studies/{id}/papers` lists the set. This proves the FR-LIT-1 ingest extension point; PDF/DOI extraction + Semantic Scholar enrichment extend these same rows in MP-10. **Item 3 - second replicated-paper recipe (FR-ANA-5):** `meyer-fragmentation` (Meyer et al., IEEE TSE 2017, DOI 10.1109/TSE.2017.2656886) - file-switch rate + focus-segment durations from `editor_focus`, exact tests, fatigue-as-perceived-productivity stand-in stated - cited in its docstring and added to the pilot plan under RQ-P3; two cited replications now back the papers-become-recipes thesis. 39 Python tests added/covered green (8 Zotero + 3 export + Meyer recipe); `uv run ruff check .` clean. **Deviations:** (1) roadmap item 3's *preferred* candidates (Ziegler persistence / GitClear churn) depend on FR-INST-17's code-evolution series, which lands with MP-12 - `meyer-fragmentation` was chosen instead because it is computable from schema-v3 `editor_focus` events already captured (rationale in the recipe docstring); persistence/churn remain the natural third replication once MP-12 lands. (2) Zotero consumed via stdlib `urllib` (no `pyzotero` dependency), consistent with D9 (adopt the *API*, not a library) and zero-bloat discipline. (3) an arXiv DataCite DOI (`10.48550/arXiv.<id>`) canonicalizes to the `arxiv:<id>` ref so a Zotero entry carrying it joins a protocol citing `arxiv:<id>` - a real join-robustness fix surfaced by the live demo (Peng et al.). (4) papers stored in a dedicated `papers` table, not shoehorned into `StoredFile` - a paper is metadata, not a byte blob. (5) demo used a local HTTP stub of the Zotero desktop API (no desktop app in this environment); the wire contract is the documented API v3, tested against recorded-shape fixtures |
| 2026-07-12 | MP-12 | FR-AGENT-1/2/3/5 ✅; FR-INST-15/16/17 ✅ (17 with a stated char-approximation); FR-ETH-2 agent-leg scoped exception implemented | The fourth instrument leg. **Agent capture (`agent-capture/`):** the Claude Code transcript JSONL is normalized (`transcript.py`, tolerant/version-defensive per the docs' "format is internal" warning, verified via the `claude-code-guide` agent) into `agent_session_meta`/`agent_turn`/`tool_call` events; the **same normalizer powers both the live hook and the post-session importer**, and because the normalized `seq` is the transcript's append-only position, hook-captured and imported events reconcile idempotently rather than duplicate (FR-ING-2) - the acceptance "middleware-down mid-session → import recovers everything" (NFR-2) falls straight out. **Content policy (`redact.py`, FR-AGENT-5):** one choke point; `metadata-only` (pilot default) stores zero conversation text (grep-the-output test), `redacted` masks literals + identifiers ≥ N chars, `full` passes through; plain-language descriptions exposed for the consent form. **Hook pack (FR-PROT-4 extended):** `protocol derive agent-hooks` emits the `.claude/settings.json` hooks section with the content policy baked from the protocol - no side channel; join keys from the runbook's `STUDY_*` env. **Session-runner instruments:** shadow-git snapshotter (`snapshot.py`, D14 → `workspace_snapshot`) with content-free participant `git_commit` observation (FR-INST-17) - deterministic git-ordinal `seq` so single-shot-per-save and long-timer modes both reconcile, and the shadow repo provably never captures the participant's `.git` internals; task harness (`harness.py`, FR-INST-16 → `task_outcome` + sticky time-to-first-green); code-evolution derivation (`evolution.py`, FR-INST-17: gross/net LOC, churn, AI-persistence). **Correlation (`correlate.py`, FR-AGENT-3):** reliance-loop spans (paste→user turn→assistant code turn→AI burst, windowed) and `edit_burst_annotation` linking bursts to the agent turn that produced them, both `derived: true`, never overwriting raw. **Middleware:** `Event` gains a `source` column; idempotency + gap detection are now per `(session, source)` so the agent leg's independent producers share the session join key without `seq` collisions and each stream's completeness is separately detectable; schema **v4** registered; existing single-producer sessions unchanged (25 API tests still green). 47 new Python tests (transcript/redaction/snapshot/harness/correlation/evolution + a live-stack end-to-end acceptance walk that also runs the RQ-P5 recipes over the emitted events); 129 total green; ruff clean; live-server demo verified. **Deviations:** (1) **event naming** conforms the producer to the already-shipped MP-07 recipe consumers (`tool_call`/`tool`, `task_outcome.firstGreenMs`) rather than the MP-12 draft table's `agent_tool_call`/`toolName`/`timeToFirstGreenMs` - the consumer contract is tested, so the new producer matches it. (2) **schema-v4 extension items deferred:** the extension-side `ai_suggestion charCount@shown` (DR-01) and `edit_burst startLine/endLine` are TypeScript changes gated by `npm run check` and out of this Python-heavy leg; FR-INST-17 persistence/churn are therefore **char-approximated and stated as such** (exactly the fallback the spec and metric-coverage review allow), and line-level persistence + accept-rate-by-size-quartile remain a focused follow-up. (3) correlation runs as an `agent-capture correlate` post-ingest job (fetch dataset → derive → POST back) rather than inside the middleware, keeping the leg's logic in the leg and the middleware unaware of it. (4) `git_commit` emitted under its own `participant-git` producer stream (distinct from the shadow snapshotter's) so each has a clean per-stream `seq`. |
| 2026-07-12 | MP-10 | FR-LIT-1/2/3/4 ✅; FR-ETH-4 ✅; FR-DASH-8 ✅ (was 🔶); D10 annotated (built); +D21 (PyMuPDF) +D22 (anthropic SDK) | The knowledge layer - pre-study elicitation on-platform. **Paper ingest (FR-LIT-1):** `POST /studies/{id}/papers` by arXiv id / DOI (Semantic Scholar metadata, D8) and `POST .../papers/upload` for a PDF (local PyMuPDF text + title guess, D21, then S2 title-search enrichment - a scanned PDF degrades to metadata-only). Text is chunked into a SQLite **FTS5** index (a build decision - no vector DB at this scale; LIKE-scan fallback when FTS5 is absent). `DELETE .../papers/{ref}` removes the paper, its edges, its links, and its index rows. **Literature graph (FR-LIT-2):** per-ingested-paper edge harvest of S2 `references`/`citations`/`recommendations` (top-50 by citationCount) into `paper_edges` with denormalized stub nodes for un-ingested suggestions; every S2 response cached in `s2_cache` so the graph renders offline after first fetch (NFR-7, live-verified); `GET .../papers/graph` returns ingested (solid) + suggested (hollow) nodes and typed edges. Dashboard `Knowledge.svelte` renders it as a hand-rolled deterministic force graph (`lib/graph.ts`, no d3-force - decision D17 discipline; unit-tested, D19), with add-by-id / PDF-upload, a detail drawer, and click-a-suggestion→ingest→regrow. **Paper↔protocol links (FR-LIT-3):** `paper_links` seeded from the protocol's `literature:` `justifies` list on ingest (so links join by construction) and editable via `GET/PUT .../papers/{ref}/links` and the drawer; surfaced in the paper list for the traceability chips. **Assistant (FR-LIT-4):** `POST .../assistant` runs a hand-rolled Claude tool-use loop (`claude-sonnet-5`, D10, verified against the `claude-api` skill) over three tools - `search_papers` (FTS), `get_protocol`, `get_dataset_summary`; the system prompt requires a `[source]` tag on every claim, extracted into clickable citation chips. **FR-ETH-4 is enforced in code, not the prompt:** `assistant.py` exposes no tool that can return a `participantId`/`sessionId`/`seq`/raw payload - the model cannot leak what it cannot see (grep-the-output test asserts `dataset_summary` carries only per-condition counts and metric mean/median). Absent `ANTHROPIC_API_KEY` the endpoint returns a graceful 503 and every other view stays offline. 11 middleware tests (S2 mocked with recorded fixtures; scripted fake Claude for one end-to-end cited exchange) + 6 dashboard graph tests; 140 Python + 25 dashboard tests green, ruff + `npm run check` clean; live-server smoke verified graph/links/graceful-503 routing. **Deviations:** (1) PDF upload is a sibling route `POST .../papers/upload` (multipart) rather than content-negotiated on the same path as the id form - FastAPI can't cleanly branch one route on content type. (2) D10's "temperature low" clause is now **unsatisfiable and dropped**: current models 400 on `temperature`/`top_p`, so determinism is steered by prompting (recorded in D10). (3) charting uses a hand-rolled force layout, not d3-force/react-force-graph (the mega-prompt's suggestions), to honour D17's build-the-marks discipline and avoid a dependency for one view. (4) the live network demo against real Semantic Scholar / a real Claude key is deferred (no outbound network or key in this environment, consistent with prior phases); the recorded-fixture tests are the reproducible acceptance demonstration, and offline routing was live-verified. |
| 2026-07-12 | MP-11 | FR-ANA-6 ✅; FR-META-1 ✅ (was 🔶); FR-META-2 ✅; D23 (tectonic) recorded | The write-up phase becomes a build artifact. **Part A - paper export (FR-ANA-6):** `analysis paper <protocol> --study <id>` emits `paper/draft.md` + `draft.tex` (article/natbib) + `references.bib` + per-RQ figures, **deterministically, no LLM in the pipeline** (NFR-6) - methods are *synthesised from the frozen protocol* (participants plan, counterbalancing, session structure, instrument configs with real probe intervals/thresholds, the cognitive-load-9 metric set with definitions + citations, ethics), results carry each recipe's methods text + booktabs tables + `\includegraphics` figures + NFR-8 stats verbatim, related work is seeded from FR-LIT-3 links (ingested-paper metadata when the middleware is up, protocol `literature:` offline), and every claim carries a `%% trace:` comment. A protocol field the methods section needs but lacks is logged as an **RQ-F1 specification defect** (FR-META-1) and left as a visible `\todo` gap rather than silently dropped. **Part B - operational findings (FR-META-1):** the middleware `findings` table + `POST/GET /findings` + `POST /studies/{id}/findings/scan` (idempotent on `(kind, context)`) auto-writes seq-gap (FR-ING-3) and gate-block (FR-PROT-3) rows; the analysis runner + `analysis paper` post recipe requires-fails / spec defects; the dashboard derives a task-board card per open finding not already carded from the status doc. **Part C - retrospective (FR-META-2):** `analysis retrospective` collects the findings log + facilitator `findings.md` + aggregate coverage and has Claude (`claude-sonnet-5`, D10 bounds, verified against the `claude-api` skill) draft an **inert** changelist proposal (SRS amendments / protocol-schema / instrument-config / rejected ideas, each citing its findings); `build_prompt` is the single FR-ETH-4 choke point (grep-the-output test: findings + aggregates only, zero participant rows); no key → an evidence bundle + hand-drafting template. **Acceptance proven:** on the seeded demo, `draft.tex` compiles to a 12-page PDF via **tectonic** (`test_draft_compiles_to_pdf_when_a_tex_engine_is_present`, skips where no TeX engine is on PATH), the golden-file test regenerates byte-identically, a seq gap + a failed requires-check yield two findings rows + two task-board cards + both cited in the retrospective evidence, and the retrospective prompt leaks zero participant data. 168 Python + 28 dashboard tests green; ruff + `npm run check` clean. **Deviations:** (1) the "run **pdflatex** to prove it" acceptance was satisfied with **tectonic** (self-contained, runs the full latex+bib+xdvipdfmx pipeline, fetches only needed packages - zero-bloat over a ~4GB MacTeX; decision D23); the compile test prefers tectonic and falls back to pdflatex+bibtex if present, and skips cleanly in CI. Overfull-hbox warnings on wide result tables are cosmetic, not errors. (2) the live Claude-drafted proposal is exercised via a scripted fake client (no key in this environment, consistent with prior phases); the offline evidence-bundle path and the FR-ETH-4 boundary are the reproducible acceptance demonstrations. (3) a real bug was found and fixed while verifying: the `finding` card kind was added to the dashboard's `deriveCards` but never registered in `TaskBoard.svelte`'s `Record<CardKind,string>` label map, which broke `npm run check` - fixed + covered by three new `derive.test.ts` cases. |
| 2026-07-12 | Post-rebase hardening (teammate metrics gate merged) | no requirement-status changes; MP-08 deviation (2) closed | Rebased the MP-06..MP-12 work onto the teammate's metrics refactor + CI coverage gate (`--cov=metrics/src --cov-fail-under=95`; passing at 98.6%). `scripts/smoke.sh` extended through `analysis paper` + a tectonic PDF compile when a TeX engine is present - closing MP-08's documented deviation (2) ("smoke chain ends at report generation"). CI gains a dashboard job (`npm run check`), so all four gated components are enforced on push. Bug found by the smoke run and fixed: a stale SQLite file (e.g. a compose `study-data` volume predating MP-12's `events.source` column) passed `/health` then 500'd on first ingest; the middleware now fails loudly at startup with reset/migration instructions (NFR-1/NFR-2 - sensor-facing failures are never quiet; nothing is migrated or deleted automatically), regression-tested. Operator documentation consolidated: new root `RUNBOOK.md` (every component + end-to-end walkthroughs + troubleshooting, all commands executed and verified), README + PROJECT_GUIDE refreshed to built status. |
| 2026-07-16 | MP-01 (rev 5, deployment elicitation) + FR-OPS build | added FR-OPS-1..4 (all 🔶: pipelines/manifests built, account provisioning is the operator's half) + decisions D24-D28; RUNBOOK gains §9 (deployment & releases) | Gap elicited by Idrees: the platform must be *live* (hosted demo + persistent dev instance), release-managed (tagged RCs → releases from the GitHub repo), and the extension Marketplace-installable, so any researcher in industry or academia can adopt it with minimal setup - funded **entirely by GitHub Student Pack benefits at $0** (the cost constraint is recorded in the requirement). Adopted: GitHub Actions/GHCR/Releases (D24), Render free tier for the self-reseeding demo (D25), Azure for Students VM + on-demand deallocated SonarQube VM + Caddy TLS (D26), VS Code Marketplace via vsce (D27). Rejected with rationale (D28): DigitalOcean (pack credits sunset 2026-07-31), Heroku (ephemeral FS would force a SQLite→Postgres port to fit the host), Fly.io, Railway, Supabase (NFR-5: third-party cloud for study data), Clerk (one-operator dashboard already has token auth), Blackfire (no open performance requirement). Priorities S/C - no Must displaced (golden rule 6). Deliverables: `deploy.yml` (main → GHCR image + demo deploy after CI green), `release.yml` (tag → gates → image + .vsix + GitHub Release; RC = pre-release + demo only; final = approval-gated VM deploy + Marketplace publish), `sonar-vm.yml` (start/deallocate), `render.yaml`, `deploy/compose.prod.yml` + `Caddyfile`, `start_with_seed.sh` (idempotent boot reseed), extension `repository` field |
| 2026-07-16 | MP-01 (rev 6, open-source refinement) | added NFR-11 (two-layer docs) + FR-OPS-5 (pluggable sign-in); decisions D29 (Clerk optional - partially supersedes D28), D30 (v0 as design tool, D15 stands), D31 (public-docs architecture) | Elicited from the owner: the repo goes open source and must read as a product, not an RE exercise - "lay-man understandable, highest quality" - while the professor-facing RE record stays complete. Direction settled via four explicit owner decisions (2026-07-16): two-layer IDs, Svelte kept with v0 as design tool, pluggable auth with optional Clerk, archive internals + rewrite public docs. Executed in the same pass: `docs/archive/roadmap/` → `docs/archive/docs/archive/roadmap/`, root `PROJECT_GUIDE.md` → `CONTRIBUTING.md`, README rewritten as front page, RUNBOOK/TOUR de-jargonized. IDs remain in `requirements/`, `docs/archive/`, and code comments (in-code traceability) - golden rule 2 (stable IDs) untouched |
| 2026-07-12 | FR-DASH-9 (plain-language layer) | FR-DASH-9 ✅ (added + built same day) | Requirement elicited from the owner's own onboarding failure ("I can no longer decode FR-PROT-7"): the dashboard now explains itself. **Middleware:** `redocs.py` parses `srs.md` + `glossary.md` live (documents of record - tooltip text cannot drift, per the requirement's rationale); `GET /requirements` + `GET /glossary` behind the same view auth; `MIDDLEWARE_REQUIREMENTS_DIR` setting; Dockerfile ships `requirements/`; smoke step asserts `/requirements` serves FR-DASH-9 itself. **Dashboard:** `lexicon.svelte.ts` loads both feeds once and backs (a) `TraceChip` - every requirement/RQ pill on every view now hovers to its SRS text - and (b) `Tip` for domain terms; `tour.svelte.ts` + `GuidedTour.svelte` drive a 12-stop first-visit (localStorage-keyed, re-launchable via "✦ Take the tour") walk of every view, each stop anchored to a `data-tour` element with plain-language copy mirrored in the new root `TOUR.md`; missing endpoints degrade to bare IDs (no hard dependency). Demo seed gains `sample-session-3.jsonl` (P02 unassisted) so both conditions render during the tour's metrics stop. Verified: 225 Python + 44 dashboard tests green, ruff + `npm run check` clean, live local boot served 73 requirements / 39 glossary terms / SPA, replay idempotent incl. session 3. **Deviation:** none - post-sprint S-priority enhancement, no Must displaced (golden rule 6) |
| 2026-07-16 | LLM provider swap (FR-LIT-4, FR-META-2) | D10 + D22 superseded by D32; requirement statuses unchanged | Owner decision: the knowledge assistant and retrospective move from the Claude API to **Gemini / Mistral free tiers** (`GEMINI_API_KEY` preferred, then `MISTRAL_API_KEY`; models `gemini-flash-latest` / `mistral-small-latest`), called over plain REST via stdlib `urllib` - no vendor SDK (`anthropic` removed from the lockfile). **The FR-ETH-4 boundary is unchanged:** both providers run the same hand-rolled tool-use loop over the same three tools (`search_papers`, `get_protocol`, `get_dataset_summary`); the grep-the-output tests still pass, and the recorded assistant exchange is now parameterized over both providers' wire shapes. Graceful degradation preserved: no key → assistant 503 with plain-language message, retrospective emits the offline evidence bundle. Keys are runtime-env only (Render env / VM `deploy/.env`), never git/CI. 240 Python tests green, ruff clean. |
| 2026-07-16 | Dashboard theming + v0 iteration loop | added FR-OPS-6 (✅ same day); D30 rev 2 (v0 gained Svelte support - direct iteration); FR-ETH-4 rev 2 (provider wording, D32); requirement-ID chips became neutral "i" info toggles (FR-DASH-6 satisfied via the toggle's trace panel) | Owner requests: (1) **theme system** - light/dark/system modes with a nav toggle, persisted (`dashboard.theme`), pre-paint stamp in `index.html` (no flash), `data-theme` attribute over the existing dataviz token sets (charts already referenced roles, so both palettes apply untouched), fluid cross-fade transitions gated on `prefers-reduced-motion`, fluid type (`clamp()`) and a <720px top-bar collapse; (2) **separate-origin dashboard for v0 vibe-coding** - `VITE_API_BASE` in `api.ts` + `MIDDLEWARE_CORS_ORIGINS` opt-in allow-list (off by default, per-origin, regression-tested: no header cross-origin unless listed). Gates green: dashboard `npm run check`, `uv run pytest` + ruff. |
